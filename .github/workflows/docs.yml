name: Docs

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  lint-docs:
    name: Lint Docs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          path: ~/.local/share/pnpm/store/v3
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}  # Changed the key to force a new cache
          restore-keys: |
            ${{ runner.os }}-node-
      - uses: FuelLabs/github-actions/setups/node@master
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest
          run_install: true
        env:
          PNPM_HOME: ~/.local/share/pnpm
          pnpm-store-path: ~/.local/share/pnpm/store/v3
      - name: Debug Info
        run: |
          pwd
          ls -la
          ls -la node_modules
      - name: Lint Check
        run: |
          pnpm install
          pnpm lint:check

  markdown-link-check:
    name: Check Links
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - uses: gaurav-nelson/github-action-markdown-link-check@1.0.12

  docs-hub-check:
    name: Docs Hub Checks
    uses: FuelLabs/github-actions/.github/workflows/mdbook-docs.yml@master
    with:
      docs-src-path: 'docs/src'
      spellcheck-config-path: 'docs/.spellcheck.yml'
