{
  "name": "breaking-change-log",
  "version": "1.0.0",
  "scripts": {
    "lint:check": "markdownlint --config .markdownlint.yaml 'breaking-changes-log.md'",
    "lint:fix": "markdownlint --config .markdownlint.yaml 'breaking-changes-log.md' --fix",
    "docs:update": "node src/index.mjs --from-workflow"
  },
  "dependencies": {
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@octokit/rest": "^21.0.1",
    "axios": "^1.7.2",
    "markdownlint-cli": "^0.37.0",
    "semver": "^7.6.3"
  }
}
